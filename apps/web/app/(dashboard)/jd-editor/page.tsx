import { JDEditorWorkbench } from '@/components/jd-editor/jd-editor-workbench';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Job Description Editor — JD Suite' };

export default function JDEditorPage() {
  return <JDEditorWorkbench />;
}
