import { SonificatorView } from '@/components/studio/sonificator-view';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Sonificator — JD Suite',
};

export default function StudioPage() {
  return <SonificatorView />;
}
